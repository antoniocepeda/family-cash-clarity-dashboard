# Firebase App Hosting configuration for the Next.js app in this directory.
# The App Hosting backend should use "app" as the repo-relative root directory.
runConfig:
  maxInstances: 10
  minInstances: 0
  cpu: 1
  memoryMiB: 512
  concurrency: 80
